apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
  namespace: rabbitmq-system
  labels:
    app: rabbitmq
spec:
  type: ClusterIP
  ports:
    - port: 5672
      name: amqp
    - port: 15672
      name: management
  selector:
    app: rabbitmq